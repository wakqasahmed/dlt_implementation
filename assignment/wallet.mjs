let privateKey = null;
let publicKey = null;
let balance = null;

const init = function (args) {};

const getPublicKey = function () {};

const checkBalance = function () {};

export default { publicKey, balance };
